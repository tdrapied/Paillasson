'use_strict';

/**
 * @param {Client} client
 * @param {Reaction} react
 * @param {User} user
 */
module.exports = (client, react, user) => {

    if (user.bot) return;

    console.log('reaction?')

};
